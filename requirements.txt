# Lean requirements for the orchestrator (API + Celery worker) - safe to
# deploy on a resource-limited host (Render free tier, etc). This assumes
# LLM/image/video/generation all happen remotely (Ollama + the Colab/Kaggle
# notebooks in ../notebooks/), which is the default now that IMAGE_API_URL,
# WAN_API_URL and a remote OLLAMA_URL are all supported.
#
# Want to run SDXL/Whisper/etc. locally instead? Also install
# requirements-local-gpu.txt - but that needs a real GPU-capable machine,
# not a free-tier web host.

# --- Core API ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.4
pydantic-settings>=2.2.1

# --- Database ---
sqlalchemy>=2.0.28
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.1

# --- Task queue ---
celery>=5.3.6
redis>=5.0.3

# --- HTTP / misc ---
httpx>=0.27.0
python-multipart>=0.0.9
jinja2>=3.1.3

# --- Media processing (lightweight - just shells out to the ffmpeg binary) ---
ffmpeg-python>=0.2.0
Pillow>=10.3.0

# --- Text to speech (online fallback; local `piper` CLI is optional and separate) ---
gTTS>=2.5.1
