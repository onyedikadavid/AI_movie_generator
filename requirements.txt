# --- Core API ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.4
pydantic-settings>=2.2.1

# --- Database ---
sqlalchemy>=2.0.28
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.1

# --- Task queue ---
celery>=5.3.6
redis>=5.0.3

# --- HTTP / misc ---
httpx>=0.27.0
python-multipart>=0.0.9
jinja2>=3.1.3

# --- Media processing ---
ffmpeg-python>=0.2.0
Pillow>=10.3.0

# --- Speech to text (local, CPU or GPU) ---
faster-whisper>=1.0.1

# --- Image generation (local diffusion) ---
torch>=2.6.0
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.28.0
safetensors>=0.4.2

# --- Text to speech ---
# Primary: piper-tts CLI binary (install separately - see README; apt/brew or
# https://github.com/rhasspy/piper/releases). gTTS is kept as an optional
# online fallback; ffmpeg silent-clip generation is the offline fallback used
# automatically by TTSService if neither is available.
gTTS>=2.5.1

# --- Optional: pose/depth guidance for character-consistent video motion ---
# Heavy extra dependency, only needed if you want SpatialService's pose maps
# to do more than gracefully no-op. Uncomment if you have the VRAM for it.
# controlnet-aux>=0.4.0
